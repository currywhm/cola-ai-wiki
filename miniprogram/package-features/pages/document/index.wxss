.reader-page { padding-top: 0; }
.reader-copy { min-width: 0; }
.reader-title { display: block; font-size: 32rpx; font-weight: 700; line-height: 1.4; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.reader-head { gap: 12rpx; margin-bottom: 20rpx; }
.organize-card { margin-bottom: 20rpx; padding: 22rpx; }
.organize-heading { display: flex; align-items: center; justify-content: space-between; }
.organize-label { color: #08736b; font-size: 23rpx; font-weight: 700; }
.organize-method { font-size: 21rpx; }
.organize-title { display: block; margin-top: 10rpx; color: #202124; font-size: 30rpx; font-weight: 700; line-height: 1.4; }
.organize-summary { display: block; margin-top: 10rpx; color: #5f6470; font-size: 25rpx; line-height: 1.65; }
.tag-list { display: flex; flex-wrap: wrap; gap: 8rpx; margin-top: 14rpx; }
.tag-list .tag { padding: 5rpx 12rpx; font-size: 21rpx; }
.point-list { display: flex; flex-direction: column; gap: 6rpx; margin-top: 14rpx; }
.point { color: #4d5360; font-size: 23rpx; line-height: 1.5; }
.organize-pending { margin: -4rpx 4rpx 18rpx; color: #078b50; font-size: 22rpx; }
.document-content { min-height: 680rpx; line-height: 1.9; font-size: 29rpx; white-space: pre-wrap; word-break: break-word; }
.article-content { white-space: normal; }
.article-content img { max-width: 100%; height: auto; }
.reader-text { display: block; white-space: pre-wrap; }
/* 真实页码：只有 PDF 这类原文自带分页符时才出现，Word / Excel 不编造页码 */
.page-flag { display: block; margin: 30rpx 0 8rpx; color: #08736b; font-size: 22rpx; font-weight: 600; letter-spacing: .5rpx; opacity: .72; }
/* 滑到底自动追加下一批，这里是提示而不是按钮 */
.reader-more { display: block; margin-top: 24rpx; color: #9aa0ac; font-size: 22rpx; text-align: center; }

/* 原文预览入口：沿用卡片语言（白底 + 1rpx 描边 + 20rpx 圆角），不加重底色 */
button.preview-entry { display:flex; justify-content:flex-start; align-items:center; gap:18rpx; width:100%; margin:0 0 20rpx; padding:22rpx 24rpx; text-align:left; background:#fff; border:1rpx solid #e6e2da; border-radius:20rpx; box-shadow:0 4rpx 16rpx rgba(31,38,55,.035); }
button.preview-entry.busy { opacity:.6; }
.preview-entry-icon { flex:0 0 auto; display:flex; align-items:center; justify-content:center; width:68rpx; height:68rpx; border-radius:18rpx; background:#f2f0eb; }
.preview-entry-copy { display:flex; flex-direction:column; gap:4rpx; min-width:0; }
.preview-entry-title { color:#202a38; font-size:29rpx; font-weight:600; line-height:1.35; }
.preview-entry-desc { color:#737b8c; font-size:22rpx; line-height:1.45; white-space:normal; }
/* 无正文时的说明卡：不要继承 document-content 的行高与最小高度，否则中间空一大块 */
.reader-empty { display:flex; flex-direction:column; gap:10rpx; min-height:0; padding:40rpx 30rpx; white-space:normal; }
.reader-empty-title { color:#202a38; font-size:28rpx; font-weight:600; line-height:1.4; }
.reader-empty-desc { color:#737b8c; font-size:24rpx; line-height:1.7; }
