/* 组件 wxss 不允许标签选择器（旧写法 button.hd-xxx 会报 “Some selectors are not allowed in component wxss”），
   但微信基础库对 <button> 的默认样式是 wx-button[class]（特异性 0,1,1），单类选择器压不住，
   会表现为「按钮没边框、文字居中、padding/宽度丢失」。
   因此这里把按钮类名连写两遍（如 .hd-new.hd-new）把特异性提到 0,2,0，等价于原来的 button.hd-new。 */
/* 多选操作条：贴在底部，和底部导航同一高度感；只用文字 + 一个实心主按钮 */
.pb-bar { display: flex; align-items: center; gap: 22rpx; flex-shrink: 0; min-height: 104rpx; padding: 8rpx 8rpx calc(8rpx + env(safe-area-inset-bottom)); background: #fff; }
.pb-text.pb-text { flex: 0 0 auto; width: auto !important; min-width: 0 !important; margin: 0; padding: 12rpx 6rpx; border: 0; background: transparent; color: #3d444c; font-size: 28rpx; font-weight: 500; line-height: 1.3; }
.pb-text.pb-text::after { border: 0; }
.pb-text.pb-text:active { opacity: .6; }
.pb-count { flex: 1 1 auto; min-width: 0; color: #9aa1a9; font-size: 25rpx; line-height: 1.3; text-align: right; }
.pb-share.pb-share { flex: 0 0 auto; width: auto !important; min-width: 0 !important; margin: 0; padding: 16rpx 44rpx; border: 0; border-radius: 999rpx; background: #20242b; color: #fff; font-size: 29rpx; font-weight: 600; line-height: 1.3; }
.pb-share.pb-share::after { border: 0; }
.pb-share.pb-share.off { background: #e6e8ea; color: #a8adb3; }
.pb-share.pb-share:active { opacity: .86; }
