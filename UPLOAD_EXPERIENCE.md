# 资料导入体验与云盘接入

## 已上线的导入路径

小程序的“添加资料”始终先让用户选择来源：

1. 从微信文件选择：适合已在聊天、文件传输助手或群文件中的 PDF、Word、文本和表格。
2. 从相册导入图片：适合保存的截图、票据、书页与白板照片。
3. 拍照扫描文字：适合现场纸质资料，上传后由服务端中文 OCR 提取文字。

微信小程序没有读取任意手机目录的权限，因此不能承诺打开 iOS“文件”或 Android 任意路径。把文件转发至微信后选择，或使用相册/拍照，是符合微信权限模型且用户可理解的路径。

支持格式：PDF、DOCX、TXT、Markdown、CSV、JPG、JPEG、PNG、WebP。单文件最大 50 MB。上传与解析失败分别显示，不会把 OCR 失败误报为成功。

## 百度网盘及其他云盘

云盘不是本地文件选择器的替代品，必须经用户授权后由服务端按授权范围读取。要上线百度网盘接入，需要以下内容：

1. 在百度网盘开放平台注册应用，取得服务端保存的 Client ID、Client Secret 和已备案的 HTTPS 回调地址。
2. 小程序只发起授权，不持有 Client Secret；后端保存每位用户加密后的 refresh token。
3. 后端分页列出用户授权范围内的文件，用户确认后由后端下载、检查文件大小和类型，再进入现有解析流程。
4. 断开授权时同时删除 refresh token；导入的资料保留或删除必须由用户明确选择。
5. 只有当服务端完成配置且健康检查通过时，前端才显示“百度网盘”入口。未配置时不显示，避免无效按钮。

同一模式可扩展至 WebDAV、腾讯微云和企业网盘。网盘文件应复制到服务端受控存储后解析，不应把用户的临时下载链接暴露给小程序或模型服务。
